title = "gc custom gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "exclude tmp/gh-cli reference code (GitHub CLI source) and dist/ build artifacts (third-party package contents) from secret scanning"
paths = [
    '''tmp/gh-cli/.*''',
    '''dist/.*''',
]
